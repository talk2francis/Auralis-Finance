import { Card, PageHero } from "../../../components/marketing";

export default function Security(){const rules=["One key, used once — no persistent signing key on Auralis servers.","No autonomous execution without explicit approval, ever.","No auto-deploy of new contracts — only audited deployments are wired.","No tight loops — every off-chain agent is rate-limited and cached.","Everything cached and rate-limited — no surprise on-chain bursts."]; return <main><PageHero eyebrow="Security" title="Non-custodial by design." narrow>Auralis never holds funds and never holds keys. The user approves every action; the agent only advises.</PageHero><section className="mx-auto max-w-3xl px-4 pb-24"><h2 className="font-display text-3xl">The five safety rules</h2><div className="mt-5 grid gap-2">{rules.map((r,i)=><Card key={r} className="flex items-center gap-4 p-4"><span className="grid h-7 w-7 shrink-0 place-items-center rounded-lg bg-[var(--teal-wash)] text-xs font-semibold text-[var(--teal)]">{i+1}</span><span className="text-[15px]">{r}</span></Card>)}</div><h2 className="mt-12 font-display text-3xl">Guardrails</h2><p className="mt-3 text-lg leading-8 text-[var(--text-secondary)]">Seven hard policy limits run before any execution: per-asset cap, per-protocol cap, liquidity floor, slippage limit, confidence floor, cooldown, and a human-approval threshold. Failing any one blocks the action.</p><h2 className="mt-12 font-display text-3xl">Responsible disclosure</h2><p className="mt-3 text-lg leading-8 text-[var(--text-secondary)]">Report a vulnerability at <a className="text-[var(--teal)]" href="mailto:security@auralis.finance">security@auralis.finance</a>. We respond inside 24 hours.</p><Card className="mt-12 bg-[var(--surface-muted)] shadow-none"><div className="text-xs font-semibold uppercase tracking-wide text-[var(--text-secondary)]">Notice</div><div className="mt-2 text-sm leading-6 text-[var(--text-secondary)]">Auralis provides risk information, not financial or legal advice. Compliance verdicts are tooling for your own due diligence, not a substitute for it.</div></Card></section></main>}

import { Award, Database, ShieldCheck } from "lucide-react";
import { Card, PageHero } from "../../../components/marketing";

export default function Business(){const cards=[[Database,"Auralis Intelligence API","Ratings, eligibility, and risk telemetry by subscription."],[Award,"Premium treasury tier","Configurable guardrails, white-label, audit log exports."],[ShieldCheck,"Attestation fees","Small per-mint fee on on-chain compliance attestations."]] as const; return <main><PageHero eyebrow="Business model" title="Trust asymmetry blocks RWA adoption." narrow>Tokenized real-world assets are growing fast. What is missing is a credible, verifiable risk layer. That is what Auralis sells.</PageHero><section className="mx-auto max-w-5xl px-4 pb-24"><div className="grid gap-4 md:grid-cols-3">{cards.map(([Icon,t,sub])=><Card key={t}><div className="grid h-10 w-10 place-items-center rounded-xl bg-[var(--teal-wash)] text-[var(--teal)]"><Icon size={20}/></div><div className="mt-4 font-display text-xl">{t}</div><div className="mt-2 text-sm leading-6 text-[var(--text-secondary)]">{sub}</div></Card>)}</div><h2 className="mt-12 font-display text-3xl">Revenue-first, not token-first.</h2><p className="mt-3 text-lg leading-8 text-[var(--text-secondary)]">No utility token, no farming, no extractive launch. The business sells a useful product to people who already need it.</p></section></main>}

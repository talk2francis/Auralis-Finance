import { getRatings } from "../../../lib/api";
import { Card, CardContent, RatingSeal } from "@auralis/ui";
export default async function Ratings(){const ratings=await getRatings();return <main className="mx-auto max-w-6xl px-4 py-16"><h1 className="font-display text-5xl">Ratings</h1><div className="mt-8 grid gap-3">{ratings.map((r)=><Card key={r.assetId}><CardContent className="flex items-center justify-between p-4"><div className="flex items-center gap-3"><RatingSeal grade={r.grade} size="sm"/><div><div className="font-medium">{r.symbol}</div><div className="text-sm text-[var(--text-secondary)]">Risk {r.riskScore} · APY {r.riskAdjustedApy}%</div></div></div><span className="text-xs text-[var(--text-secondary)]">{r.ratingHash.slice(0,10)}…</span></CardContent></Card>)}</div></main>}

"use client";
import { useMemo, useState } from "react";
import { Button, Card, CardContent, CardHeader, CardTitle, ConfidenceMeter, Slider, StateWrapper, StatusPill } from "@auralis/ui";
import { Donut } from "../../../../components/marketing";
import { TxButton } from "../../../../components/TxButton";

const assets = [
  { symbol: "USDY", current: 42, risk: 28, apy: 4.8 },
  { symbol: "mETH", current: 31, risk: 34, apy: 3.2 },
  { symbol: "USDe", current: 27, risk: 48, apy: 8.4 },
];
const tabs = ["Conservative", "Balanced", "Yield tilt"] as const;
export default function SimulatorPage(){
  const [scenario,setScenario]=useState<typeof tabs[number]>("Balanced");
  const [targets,setTargets]=useState<Record<string,number>>({ USDY: 45, mETH: 30, USDe: 25 });
  const [review,setReview]=useState(false);
  const total = Object.values(targets).reduce((a,b)=>a+b,0);
  const bad = total !== 100 || Math.max(...Object.values(targets)) > 50;
  const policyRows = [["Total allocation = 100%", total === 100, `${total}%`],["Max single asset ≤ 50%", Math.max(...Object.values(targets)) <= 50, `${Math.max(...Object.values(targets))}%`],["AI confidence ≥ 70", true, "82%"],["Liquidity score ≥ 60", true, "78"],["Slippage ≤ 0.50%", true, "0.22%"]] as const;
  const impact = useMemo(()=>({ apy:(assets.reduce((s,a)=>s+a.apy*(targets[a.symbol]??0),0)/100).toFixed(2), risk:(assets.reduce((s,a)=>s+a.risk*(targets[a.symbol]??0),0)/100).toFixed(1), liquidity:"$42.8M", tx:"~0.03 MNT" }),[targets]);
  function setTarget(symbol:string,value:number){ setTargets((t)=>({...t,[symbol]:value})); }
  function applyScenario(s: typeof scenario){ setScenario(s); setTargets(s==="Conservative"?{USDY:50,mETH:35,USDe:15}:s==="Yield tilt"?{USDY:30,mETH:25,USDe:45}:{USDY:45,mETH:30,USDe:25}); }
  return <div className="mx-auto max-w-7xl space-y-6"><div><p className="text-sm font-medium uppercase tracking-[0.12em] text-[var(--teal)]">Simulator</p><h1 className="font-display text-4xl">Rebalance simulator</h1><p className="mt-2 text-[var(--text-secondary)]">Preview allocations and enforce AuralisPolicyGuard before any user-signed transaction.</p></div><div className="inline-flex rounded-[12px] border border-[var(--border)] bg-[var(--surface-muted)] p-1 shadow-[var(--shadow-soft)]">{tabs.map(t=><button key={t} className={`rounded-[10px] px-4 py-2 text-sm font-medium ${scenario===t?"bg-[var(--surface)] text-[var(--teal)] shadow-[var(--shadow-soft)]":"text-[var(--text-secondary)]"}`} onClick={()=>applyScenario(t)}>{t}</button>)}</div><StateWrapper status="populated"><section className="grid min-w-0 gap-4 lg:grid-cols-[minmax(0,1fr)_340px]"><Card><CardHeader><CardTitle>Current vs proposed</CardTitle></CardHeader><CardContent className="space-y-6"><div className="grid gap-6 sm:grid-cols-2"><div><p className="mb-2 text-sm text-[var(--text-secondary)]">Current portfolio allocation</p><Donut /></div><div><p className="mb-2 text-sm text-[var(--text-secondary)]">Proposed portfolio allocation</p><div className="grid min-h-40 place-items-center rounded-[10px] bg-[var(--surface-muted)] p-4"><div className="grid h-36 w-36 place-items-center rounded-full" style={{background:`conic-gradient(var(--teal) 0 ${targets.USDY}%, #1F58A8 ${targets.USDY}% ${targets.USDY+targets.mETH}%, #8C97A8 ${targets.USDY+targets.mETH}% 100%)`}}><div className="grid h-24 w-24 place-items-center rounded-full bg-[var(--surface)] text-center"><span className="font-display text-xl">{total}%</span><span className="-mt-5 text-xs text-[var(--text-secondary)]">target</span></div></div></div></div></div><div className="overflow-x-auto rounded-[var(--radius-card)] border border-[var(--border)]"><table className="w-full text-sm"><thead className="text-left text-xs uppercase tracking-wide text-[var(--text-secondary)]"><tr><th className="p-4">Asset</th><th className="p-4">Current</th><th className="p-4">Target</th><th className="p-4">Adjustment</th></tr></thead><tbody>{assets.map(a=><tr className="border-t border-[var(--border)]" key={a.symbol}><td className="p-4 font-medium">{a.symbol}</td><td className="p-4">{a.current}%</td><td className="p-4 font-mono">{targets[a.symbol]}%</td><td className="p-4 min-w-56"><Slider aria-label={`Target allocation for ${a.symbol}`} min={0} max={100} value={targets[a.symbol]} onChange={e=>setTarget(a.symbol,Number(e.target.value))}/></td></tr>)}</tbody></table></div><div className="flex items-center justify-between rounded-[10px] bg-[var(--surface-muted)] p-3"><span className={total===100?"text-[var(--emerald)]":"text-[var(--rose)]"}>=100% validator: {total}%</span><StatusPill status={bad?"degraded":"operational"}>{bad?"Needs adjustment":"Policy-ready"}</StatusPill></div><RoutePreview /></CardContent></Card><Card><CardHeader><CardTitle>Impact summary</CardTitle></CardHeader><CardContent className="space-y-3"><Metric label="APY" value={`${impact.apy}%`}/><Metric label="Risk" value={impact.risk}/><Metric label="Liquidity" value={impact.liquidity}/><Metric label="Tx cost" value={impact.tx}/><ConfidenceMeter value={82}/><Button className="w-full" onClick={()=>setReview(true)}>Review approval</Button></CardContent></Card></section></StateWrapper>{review&&<div className="fixed inset-0 z-50 grid place-items-center bg-black/30 p-4"><Card className="w-full max-w-xl"><CardHeader><CardTitle>Confirm sheet</CardTitle></CardHeader><CardContent className="space-y-4"><p className="text-sm text-[var(--text-secondary)]">Exact assets: {assets.map(a=>`${a.symbol} ${targets[a.symbol]}%`).join(", ")}</p><Metric label="ChainId" value="5003 Sepolia rehearsal"/><Metric label="Gas estimate" value="~0.03 MNT"/><div className="space-y-2">{policyRows.map(([label,ok,value])=><div key={label} className="flex justify-between rounded-lg border border-[var(--border)] p-2"><span>{label}</span><span className={ok?"text-[var(--emerald)]":"text-[var(--rose)]"}>{ok?"PASS":"BLOCK"} · {value}</span></div>)}</div><div className="flex justify-end gap-2"><Button variant="secondary" onClick={()=>setReview(false)}>Close</Button><TxButton disabled={bad} intent="executeRebalance">executeRebalance</TxButton></div></CardContent></Card></div>}</div>;
}
function Metric({label,value}:{label:string;value:string}){return <div className="rounded-xl border border-[var(--border)] bg-[var(--surface-muted)] p-3"><div className="text-xs font-semibold uppercase tracking-wide text-[var(--text-secondary)]">{label}</div><div className="mt-1 font-display text-2xl">{value}</div></div>}
function RoutePreview(){return <div className="rounded-xl bg-[var(--teal-wash)] p-4 text-sm"><b>Route preview:</b> reduce USDe, increase USDY, maintain mETH staking exposure. No autonomous execution.</div>}
